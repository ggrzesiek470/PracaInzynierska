import Logger from '../Logger.js';
import GlobalData from '../GlobalData.js';

let channel = "getAllHistoricalGames";

export default function getAllHistoricalGames ({ client, opers, models, io }) {
    client.on(channel, function (data) {
        




    })
}